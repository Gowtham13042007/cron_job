name: Keep Backend Awake
on:
  schedule:
    - cron: "*/12 * * * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render backend
        run: curl -fsS --max-time 10 https://moneyapp01-2.onrender.com